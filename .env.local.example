# ── Supabase ─────────────────────────────────────────────
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here   # NEVER expose this client-side

# ── Finnhub (Live Ticker Tape) ────────────────────────────
# Sign up free at: https://finnhub.io — no credit card needed
# Free tier: 60 req/min, no daily cap, US stocks REAL-TIME
# Without this key the ticker tape falls back to seed data silently.
FINNHUB_API_KEY=your_finnhub_api_key_here

# ── Stock Price API ───────────────────────────────────────
# Free tier at: https://financialmodelingprep.com/
# 250 requests/day free — more than enough for 15-min refreshes
FMP_API_KEY=your_fmp_api_key_here

# ── Cron Security ─────────────────────────────────────────
# Random secret — used to protect POST /api/prices/refresh
# Generate with: openssl rand -hex 32
CRON_SECRET=your_random_secret_here
